# 台灣運動彩券「球類運動賽事分析」網站：工作計畫 & SOP（可勾選）

> 目標：做一個「未來科技感」的前端網站，使用 `index.html` + `style.css` + `app.js`，可輸入/載入賽事資料，產生兩隊勝負可能（%）、進球/得分區間機率（%），並列出球員歷史成績做雙方評估（僅供資訊參考，非保證獲利）。

---

## 1) 需求澄清（先定規格）

- 支援運動類型（台灣運彩常見球類）：足球、籃球、棒球、排球、冰球（可先做足球 MVP，其他以同一資料結構擴充）。
- 輸入資料來源（擇一或並行）：
  - A. 手動輸入（UI 表單貼資料／CSV JSON）
  - B. 本地假資料（`app.js` 內建範例）
  - C. 之後再接 API（本期先不做網路抓取）
- 分析輸出（第一版）：
  - 勝/和/負機率（足球：含和局；其他可調整）
  - 進球/得分：例如 0–1、2–3、4+ 的機率分佈（%）
  - 重要球員對位：近 N 場數據、對戰紀錄（若有）、傷停標記（若提供）
- 風格：未來科技感（霓虹、玻璃擬態、動態網格/掃描線、數據儀表板感）

**完成定義（DoD）**
- 開啟 `index.html` 可離線運作（不依賴外網）。
- 可新增/選擇賽事，立即看到：勝負機率 + 進球/得分分佈 + 球員表格 + 對戰摘要。
- 版面在手機與桌機可用（RWD 基本可讀、可操作）。

---

## 2) 工作產出（Deliverables）

- `index.html`：版面、輸入區、分析結果區、球員表格、（可選）圖表容器
- `style.css`：未來科技感主題（色票、卡片、動態背景、RWD）
- `app.js`：資料模型、計算邏輯、UI 綁定、範例資料、匯入/匯出
- `leagueCache.js`：離線快取（避免前端暴露 API Key）
- `tools/update-cache-footballdata.ps1`：用 Football-Data.org 產生快取（建議）
- `tools/update-cache-rapidapi.ps1`：用 RapidAPI 抓原始資料（先探測欄位/驗證可用性）
- `工作計畫.md`：本文件（進度追蹤 + SOP）

---

## 3) SOP 勾選清單（進度追蹤）

> 使用方式：完成一項就把 `[ ]` 改成 `[x]`。

### A. 規格與資料（SOP-REQ）
- [x] SOP-REQ-01：確認第一版要支援的運動（預設：足球）
- [x] SOP-REQ-02：確認要不要「和局」選項（足球預設：要）
- [ ] SOP-REQ-03：確認球員欄位（例：出賽、進球/得分、助攻、射門、上場時間、近 5/10 場）
- [x] SOP-REQ-04：決定資料輸入方式（手動 / 範例 / 檔案匯入）
- [x] SOP-REQ-05：定義進球/得分區間分佈的呈現方式（表格 / 條圖）
- [x] SOP-REQ-06：確定資料源策略（不手動輸入，採「本機快取生成」）

### B. UI/UX（SOP-UI）
- [x] SOP-UI-01：完成資訊架構（左：輸入；右：結果；下：球員表格）
- [x] SOP-UI-02：設計未來科技感主題（色票、字體、元件樣式）
- [x] SOP-UI-03：RWD（手機：改成上下排列；表格可橫向捲動）
- [x] SOP-UI-04：空狀態/錯誤提示（資料不足、欄位缺漏）

### C. 資料源整合（SOP-DATA）
- [x] SOP-DATA-01：建立離線快取機制（`leagueCache.js` 注入到頁面）
- [x] SOP-DATA-02：Football-Data.org 快取生成腳本可用（環境變數帶 token）
- [x] SOP-DATA-03：RapidAPI Host 確認（`free-api-live-football-data.p.rapidapi.com`）
- [ ] SOP-DATA-04：RapidAPI 實測可用端點與欄位（跑 `rapidCache.json` 探測）
- [ ] SOP-DATA-05：RapidAPI 資料轉換成 `leagueCache.js`（不暴露 key）

### C. 分析引擎（SOP-ENG）
- [x] SOP-ENG-01：建立資料模型（Team/Player/Match/Stats）
- [x] SOP-ENG-02：計算隊伍強度指標（近況、攻防、主客、對戰；權重可調）
- [x] SOP-ENG-03：輸出勝負機率（%）（含信心/不確定度提示）
- [x] SOP-ENG-04：輸出進球/得分分佈（%）（先用簡化分佈；可再升級）
- [ ] SOP-ENG-05：球員影響（Top players / 缺陣影響 / 對位優勢摘要）

### D. 前端整合（SOP-FE）
- [x] SOP-FE-01：`index.html` 完成（結構語意化、可讀性）
- [x] SOP-FE-02：`style.css` 完成（主題、元件、動畫、RWD）
- [x] SOP-FE-03：`app.js` 完成（事件綁定、渲染、計算、狀態管理）
- [ ] SOP-FE-04：匯入/匯出（JSON 或 CSV，至少一種）

### E. 品質檢查（SOP-QA）
- [ ] SOP-QA-01：用 2–3 組範例賽事驗證（數值加總=100%、不爆版）
- [ ] SOP-QA-02：Chrome/Edge 基本相容
- [ ] SOP-QA-03：效能（表格 200+ 球員仍可用）
- [x] SOP-QA-04：文案與免責（非投資建議/非保證獲利；鼓勵理性投注）

---

## 4) 里程碑（建議）

- M1（可運作原型）：足球 1 場賽事 + 範例球員資料 + 產出機率（無圖表也可）
- M2（可用版面）：未來科技感 UI + RWD + 匯入資料
- M3（擴充）：多運動類型模板化 + 更細的得分分佈 + 更完整球員對位

---

## 5) 風險與備註

- 若沒有可靠的即時數據來源（API/手動提供），第一版將以「你輸入的資料」為準；網站負責計算與可視化。
- 機率模型會先採用「可解釋、可調權重」的簡化方式；之後可再升級（例如 Poisson/Log5/Elo 變體）。
